// Explicitly clears the header actions on soft navigation to the file viewer —
// an unmatched slot would keep showing the previous route's actions.
export default function FileHeaderActionsPage() {
  return null;
}
