// The create route has no header actions. A static `create` segment also keeps
// the slot from matching `create` as a `[workflowId]`.
export default function CreateHeaderActionsPage() {
  return null;
}
