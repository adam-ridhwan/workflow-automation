// Matching the files route explicitly clears the trail on soft navigation —
// an unmatched slot would keep showing the previous one.
export default function FilesBreadcrumbPage() {
  return null;
}
