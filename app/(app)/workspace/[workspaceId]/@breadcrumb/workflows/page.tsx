// Matching the workflows root explicitly clears the trail on soft
// navigation — an unmatched slot would keep showing the previous one.
export default function WorkflowsBreadcrumbPage() {
  return null;
}
